dependencies {

    compile(
            [project(path: ":core")],
            ['ch.qos.logback:logback-classic:1.2.3']
    )

    testCompile(
            [project(path: ":core", configuration: 'testOutput')]
    )

}